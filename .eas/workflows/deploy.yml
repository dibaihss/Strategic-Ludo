# https://docs.expo.dev/eas/hosting/workflows/ 
name: Deploy

on:
  push:
    branches: ['debug-bot-modus']

jobs:
  deploy:
    type: deploy
    name: Deploy
    environment: production
    params:
      prod: true
