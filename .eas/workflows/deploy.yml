# https://docs.expo.dev/eas/hosting/workflows/ 
name: Deploy

on:
  push:
    branches: ['feature/music-sound-effect']

jobs:
  deploy:
    type: deploy
    name: Deploy
    environment: production
    params:
      prod: true
