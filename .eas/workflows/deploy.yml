# https://docs.expo.dev/eas/hosting/workflows/
name: Deploy

on:
  push:
    branches: ['main', 'release/*']
    tags: ['v*.*.*']

jobs:
  deploy:
    type: deploy
    name: Deploy
    environment: production
    params:
      prod: true
