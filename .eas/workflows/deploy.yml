# https://docs.expo.dev/eas/hosting/workflows/ 
name: Deploy

on:
  push:
    tags: ['v*.*.*']

jobs:
  deploy:
    type: deploy
    name: Deploy
    environment: production
    params:
      prod: true
