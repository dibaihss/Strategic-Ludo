# https://docs.expo.dev/eas/hosting/workflows/  deploy to production on push to main branch
name: Deploy

on:
  push:
    branches: ['main']

jobs:
  deploy:
    type: deploy
    name: Deploy
    environment: production
    params:
      prod: true
